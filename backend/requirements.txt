# Intención: QUÉ usa el backend. Las versiones exactas viven en requirements.lock,
# que es de donde instala el Dockerfile — este fichero NO se instala en la imagen.
#
# Para añadir o subir una dependencia: tócala aquí y regenera el lock
# (ver la cabecera de requirements.lock).

# --- Web ---
fastapi
uvicorn[standard]
python-multipart
itsdangerous
Authlib

# --- Base de datos ---
sqlalchemy>=2.0
psycopg[binary]
alembic

# --- Datos ---
pydantic>=2
pandas
openpyxl
pycountry

# --- Integraciones ---
simple-salesforce
requests
httpx

# --- LLM (Moby) ---
openai>=1.35.0
anthropic>=0.40.0
